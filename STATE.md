# 현재 상태 — v1.5

기준: `main` 13b1b50 (PR #3으로 v1.0.1 병합). 작업 브랜치: `codex/v1.5-data-calibration`.
요청: 이전 연구·학습 결과를 실사용 데이터에 적용하고, 전체 Excel·가중치·등급 재산출에 선택 사항인 선호 영웅 설정과 추천 규칙 변경까지 합친 v1.5를 전달한다. GitHub 쓰기는 하지 않으며 사용자가 업로드·병합한다.

## 반영 내용

- 상성 53명·비미러 2,754개 중 명시적인 팀전 보정 18개 적용. 미러 53개는 계산상 0. 미입력 2개(도미나↔캐서디)는 임의로 채우지 않는다.
- 맵 53×30=1,590개 값과 썸네일 유지. 제공된 픽률 기반 선호 점수이며 승률이 아니다.
- 가중치: 상성 1 / 궁합 0 / 맵 0.6. 상대 탱커 항은 내 역할이 탱커면 2배, 딜러·힐러면 1.5배. 나머지 항 1배.
- `scripts/calibrate_grades.ts`: 30,000개 유효 조합×역할별 모든 후보=159만 점수, 전장별 1,000조합, 시드 20260921. 고정 역할별 5/20/40/60/80/95 백분위 경계 재산출.
- `grade_cuts.json`에 가중치·원본 해시·시드·산출 방법·실제 등급 분포 포함. 모스트 필터/현재 조합 순위로 등급을 바꾸지 않는다.
- 옛 기본 가중치는 새 기본값으로 자동 전환. 직접 조정한 값과 모스트 선택은 유지. 새 가중치 키 `ow-coach.weights.v1.5`.
- `matchups.csv`를 53명 전체로 동기화. 변환기는 탱/딜/힐 모두 처리하며 불완전 CSV·중복·잘못된 수치를 덮어쓰기 전에 거부한다.
- 버전 표기 v1.5, 패키지 1.5.0. 점수·등급·동점 순서는 유지한다.
- 첫 화면 역할 선택 → 상대·전장 화면으로 바로 이동. 선호가 없으면 해당 역할 전체 중 상위 3명 추천.
- 상대 화면의 `선호 영웅(모스트) 선택` 버튼으로 별도 설정 화면 진입. 역할별 탭에서 모스트 1~3순위·가능을 저장하며 돌아오면 원래 역할·상대·전장을 유지한다. 기존 저장 형식을 그대로 사용한다.
- 주 추천 후보는 모스트와 가능의 합집합. 점수순 최대 3명, 후보가 1~2명이면 그 인원만 표시한다. 순위 가산점은 없다. 전체 해제 시 전체 역할 추천으로 복귀한다.
- 선호 주 추천이 모두 약간 불리 이하일 때 같은 역할 전체 최고 영웅 1명을 추가 제안한다. 가능이 중립 이상이면 발동하지 않는다. 전체 최고가 주 추천에 있으면 중복 카드를 만들지 않는다.

## 경계값

| 역할 | 매우 불리 이하 | 불리 이하 | 약간 불리 이하 | 중립 이하 | 약간 유리 이하 | 유리 이하 |
|---|---:|---:|---:|---:|---:|---:|
| 탱커 | -8.40 | -4.22 | -1.08 | 1.62 | 4.72 | 8.82 |
| 딜러 | -7.76 | -3.62 | -0.52 | 2.10 | 5.12 | 8.96 |
| 힐러 | -8.42 | -4.88 | -2.12 | 0.36 | 3.14 | 6.58 |

총점이 마지막 경계를 넘으면 매우 유리. 등급은 가상 상황 점수 분포상의 위치이며 승률이 아니다. 맵 미선택도 같은 고정 경계를 사용하고 맵 항만 0이다.

## 검증 및 실행

- 변경 전 55개 테스트 통과. 현재 데이터·마이그레이션·CSV·분포·새 선호 규칙 검증을 포함한 64개 테스트 통과.
- `npm run grades:check`로 현재 데이터·가중치와 산출 결과 전체 일치 확인.
- `npm run build`, `npm run portable` 통과. 빌드 앱의 모스트 선택·추가 추천 조건·맵/가중치 변경·저장 복원을 DOM 통합 검사로 확인했고 오류 0건이었다.
- 단일 HTML을 file 경로에서 구동해 초상화 53개·전장 30개 내장, 기본값 이전, 실제 모스트 선택과 서킷 로얄 추천 9.8/9.46/8.76을 확인했다. 저장 검증은 React 효과 실행을 기다린 뒤 수행했다.
- 데이터 패치 단계의 독립 코드 검토: 수정이 필요한 결함 없음. 상성 18개·맵 값 유지·원본 해시·설정 전환·CSV 보호를 재확인했다.
- 손계산: 지브롤터, 상대 자리야·리퍼·바스티온·아나·젠야타에서 시그마 `2×2+.5+1+2+2−.4×.6=9.26`, 윈스턴 `3×2−3−3+2−2+3×.6=1.8`.
- 과거 v0.3~v0.5 결과는 별도 보존한 v1.0.1 데이터·가중치를 명시해 회귀 검증한다. v1.5 기본값의 점수 변화는 의도된 변경이다.
- 실행: `npm ci`, `npm run dev`. 배포 빌드: `npm run build`. 단일 HTML: 빌드 후 `npm run portable`.
- 데이터 수정: CSV를 편집한 경우 `npm run data:from-csv`, 그 후 `npm run grades:calibrate`, `npm test`, `npm run grades:check`, 빌드·portable 재생성.

## 전달 자료와 다음 작업

Excel은 영웅 상성 53×53, 맵 상성 53×30, 가중치·등급, 팀전 보정 18개 시트다. 데이터 JSON과 일치하는 전달 시점의 자료다. Excel만 편집해 앱에 자동 적용하는 기능은 없다.
상세 근거·한계·재산출 방법은 `docs/v1.5.md`, 보정 원문 위치는 `data/revisions/v1.5-matchup-corrections.json`을 먼저 읽는다.
아군 궁합, 조합 분류/조건부 시너지, 구간별 맵 점수는 아직 구현하지 않았다. v1.5는 기존 가산 모델을 실용적으로 보수한 버전이다. 전 영웅·전 상황의 팀전 정확도를 검증했다는 의미는 아니다.
기존 `data/raw`·원문 파서·과거 XLSX는 역사 자료이며 v1.5 생성 경로로 쓰지 않는다.

공개 사이트는 main 병합 시 GitHub Actions로 배포된다. 이 작업에서는 main 병합·배포를 실행하지 않는다.
2026-09-21 기준 main은 `13b1b50`이며 사용자가 첨부한 README·STATE는 이 커밋과 바이트 단위로 일치했다. 이번 추가 작업에서는 GitHub 쓰기를 시도하지 않았다. 사용자가 별도로 main을 변경했다면 업로드 전 차이를 확인한다.

`OW_Coach_v1.5_Updated_Upload.zip`은 위 기준 main에 적용할 데이터 패치와 이번 UI·추천 변경을 모두 포함한다. 이전 v1.5 업로드 ZIP 대신 이 파일 하나를 사용한다. `UPLOAD_FILES` 안에 있는 항목을 main에서 시작한 새 브랜치에 업로드한 뒤 PR로 병합한다. `.github` 폴더를 포함해 경로를 그대로 유지한다. ZIP·안내문·manifest 자체를 저장소에 업로드하지 않는다.

추가 화면 검증: 빌드한 단일 HTML을 DOM에서 실행해 역할 선택 직후 상대 화면, 선택형 모스트 진입, 역할별 저장·재방문 복원, 가능 추천 포함, 2명만 선택한 경우 2명 표시, 상대 5명·하바나 유지, 선호 해제 후 전체 후보 복귀를 확인했다. JavaScript 오류 0건. 화면 스크린샷에 의한 시각 검증은 이번 단계에서 수행하지 않았다.
