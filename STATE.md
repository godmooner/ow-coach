# 현재 상태 — MVP 0.5

기준: `main`의 `dfd2171b4cb22419ecfa054d4a131ada54bf7abe` (MVP 0.4, PR #1 병합).
작업 브랜치: `codex/mvp-0.5-most-selection`. 확인 날짜: 2026-09-19.
근거: 기존 모스트 선택 화면 요청 + 추가로 전달된 `HANDOFF-v0.5.md` 및 `grade_cuts_1.json`.
첨부 경계선 파일은 내용을 바꾸지 않고 `data/build/grade_cuts.json`으로 추가했다.

## 이번 버전의 범위와 통합 결정

- 진행 중이던 모스트 선택 화면과 새 인수인계의 선호 필터·등급·가중치 확장을 같은 0.5에 반영했다.
- 기존 요청의 1·2·3순위 각각 한 명, 4순위 여러 명, 전체 선택, 이름 요약 화면을 유지했다.
- 새 인수인계에 따라 **순위를 비워 두거나 0~2명만 골라도 완료 가능**하다. 이전 진행 중 버전의 “상위 3명 필수” 조건은 해제했다.
- 선택 여부는 추천 후보 필터에 적용한다. **모스트 순위에 따른 가산점·동점 우선권은 적용하지 않는다.** 이전의 “추천에 전혀 반영하지 않음” 범위는 추가 요청으로 변경되었다.
- 맵, 아군 궁합, 추천 카드의 모스트 순위 배지, 검색, 단축키, URL 공유, 초상화 최적화는 추가하지 않았다.

## 모스트 선택과 저장

- 흐름은 역할 선택 → 모스트 선택 → 완료 또는 건너뛰기 → 상대 선택이다.
- 선택한 역할의 영웅만 기존 초상화 버튼으로 표시한다. 탱커 15명, 딜러 24명, 힐러 14명이다.
- 1~3순위를 고르면 다음 빈 순위로 자동 이동하고, 모두 채워지면 4순위로 이동한다. 순위 버튼으로 직접 대상을 바꿀 수도 있다.
- 다른 순위의 영웅을 고르면 현재 순위로 이동하고 기존 자리는 비워진다. 같은 순위에서 다시 고르면 해제된다. 상위 순위에서 다른 영웅을 고르면 기존 영웅을 대체한다.
- 4순위는 인원 제한이 없다. 전체 선택은 기존 1~3순위를 유지하고 나머지를 4순위로 지정한다. 상위 순위를 임의로 채우지 않는다.
- 전체 해제와 “선택 없이 건너뛰기”는 현재 역할의 선호 목록을 비운다. 건너뛰기는 이어서 상대 화면으로 이동한다.
- 상대 화면 역할 표시 아래에 선택한 순위·이름을 표시하고 모스트 수정 버튼을 제공한다. 선택이 없으면 전체 후보라고 표시한다.
- 모스트 수정·역할 변경 중 일부 또는 전체 상대 선택은 유지된다. 상대 화면의 초기화는 상대만 비우며 모스트와 저장값은 유지한다.
- `MostSelection`은 `ids`(중복 없는 클릭 순서 배열)와 `top`(명시적으로 지정한 1~3순위)을 가진다. 4순위는 `ids`에서 `top`을 제외한 목록이다.
- 선택 순서와 사용자가 나중에 수정한 순위를 둘 다 보존하기 위해 저장을 분리했다. `ow-coach.preferred-heroes.v1`은 `{tank: [...], damage: [...], support: [...]}` 형태의 클릭 순서 배열, `ow-coach.most-ranks.v1`은 역할별 `top` 배열이다. 새로 선택하면 뒤에 추가하고, 해제 후 재선택해도 뒤에 추가한다. 기존 선택의 순위만 이동할 때에는 클릭 순서를 바꾸지 않는다.
- 두 값은 `localStorage`에 저장하고 재방문 시 복원한다. 잘못된 JSON은 안전하게 처리하고, 중복·알 수 없는 ID·다른 역할 ID는 복원에서 제외한다. 순위 정보만 손상되면 선호 배열은 유지한다.
- 브라우저 저장이 막혀도 앱은 동작하고, 현재 페이지에서만 유지된다는 안내를 표시한다. 상대 영웅은 영구 저장하지 않는다.

## 추천과 등급

- `src/recommend.ts`의 계산식은 변경하지 않았다. `ENEMY_WEIGHT`를 검증 및 화면 문구용으로 export하고, `damage.tank`, `support.tank` 두 값만 1에서 2로 바꿨다. `tank.tank`는 기존 3이며 나머지는 1이다.
- 소수 허용, 미러 0점, 누락 0점, 동점 같은 순위·영웅 목록 순서를 유지했다. `heroes.json`과 `matchups.json`은 0.4와 동일하다.
- `src/recommendationView.ts`에서 전체 계산 결과를 선호 ID로 필터한 뒤 최대 3명을 표시한다. 전체 결과의 상위 3명을 먼저 잘라 필터하지 않는다.
- 선호 0명은 전체 후보이며 전체 선택과 같은 결과다. 선호 1~2명은 있는 만큼 표시한다. 필터 후 순위는 후보 안에서 다시 매기되 점수·동점 고정 순서는 유지한다.
- 등급은 표시 계층에서만 붙인다. `grade_cuts.json`의 역할별 `cuts`를 읽어 `score <= cuts[i]`이면 `labels[i]`, 전부 초과하면 마지막 라벨이다. 숫자는 코드에 복사하지 않았다.
- 등급을 크게, 원래 총점을 작게 옆에 표시한다. 조합 내 순위, 후보 수, 모스트 순위는 등급에 영향을 주지 않는다.
- 첨부 JSON의 `enemy_weight`와 계산 코드의 가중치 표가 같은지 테스트로 확인한다. 가중치·상성 데이터를 바꾸면 등급 경계선도 검토해야 한다.
- `src/HeroVisuals.tsx`는 기존 역할 아이콘·초상화 표시·오류 폴백을 그대로 공유한다. 기존 화면 톤을 유지하며 버전은 0.5.0이다. 의존성 추가는 없다.

## 0.4에서 확정한 데이터와 딜러 회귀 기준

- `data/build/matchups.json`을 첨부 파일과 바이트 단위로 동일하게 교체했다. `heroes.json`은 기준 커밋과 동일하다.
- 출처 영웅 53명, 점수 2,754개, 그중 소수 86개. 미러 외 누락은 도미나→캐서디 및 캐서디→도미나뿐이다.
- 기존 1,190개 관계는 모두 보존되며 값 불일치는 0건이다.
- **인수인계서의 회귀 전제를 정정했다.** 딜러에서 시작하는 관계 57개가 추가되었고, 그중 45개가 0이 아니다. 기존 결측 0점을 대체하므로 같은 계산 코드에서도 일부 딜러 결과는 바뀐다.
- 이 사실을 사용자에게 설명했고, 사용자가 **새 데이터 전부 반영**을 선택했다. 기존 딜러 관계만 적용하는 호환 처리는 넣지 않았다.
- 0.4 당시의 회귀 기준은 **같은 데이터를 넣으면 0.3 계산 코드와 딜러의 전체 점수·순위·동점 순서가 같다**이다.
- 수정 전 0.3 코드로 기존/신규 데이터 각각의 기준 결과를 저장했다. 16개 상대 조합이 영웅 53명을 모두 한 번 이상 포함하며, 각 조합의 딜러 24명 전체를 비교한다.
- 기준 자료: `tests/fixtures/matchups-v0.3.json`, `tests/fixtures/recommend-v0.3.json`. 실제 상성 데이터를 갱신할 때 신규 데이터 기준 결과도 검토해야 한다. 기존 데이터 기준 결과는 보존한다.

데이터 교체로 달라지는 예: 상대 `D.Mon·안란·시에라·미즈키·제트팩 캣`.
기존 데이터는 토르비욘 6점·리퍼 5점·바스티온 4점, 신규 데이터는 애쉬 10점·위도우메이커 7점·리퍼 5점이다. 가중치 변경으로 발생한 차이가 아니다.

**데이터 갱신 주의:** 0.4에서 첨부 JSON만 교체했고, 0.5에서는 데이터를 변경하지 않았다. 기존 CSV·XLSX·원문/파서는 0.3 자료이므로 `npm run data:from-csv` 또는 기존 원문 재생성을 실행하면 신규 관계가 사라질 수 있다. 새 원천 자료와 동기화하기 전에는 이 JSON을 기준으로 사용한다.

## 검증 결과와 최신 회귀 기준

- `npm test`: 34개 통과. 추천·데이터 15개, 모스트·저장 11개, 필터·등급 8개다.
- **탱커 0.4 회귀:** 가중치 수정 전의 실제 0.4 코드로 `tests/fixtures/recommend-tank-v0.4.json`을 만들었다. 16개 상대 조합이 53명을 모두 포함하며 탱커 15명의 전체 점수·순위·동점 순서를 비교한다. 전부 일치한다.
- **딜러 회귀 기준 변경:** 0.5에서는 상대 탱커 2배가 의도된 변경이므로 0.3/0.4 딜러 결과와 완전 일치를 요구하지 않는다. 기존 0.3 원본/확장 데이터 기준 자료를 보존하고, 모든 조합에서 오직 탱커 항 한 번만 추가한 결과와 일치하는지 검증한다.
- 손계산 상대: `D.Mon·애쉬·겐지·아나·메르시`. 딜러 정크랫은 `3×2 - 2 + 0 + 2 + 2 = 8`, 힐러 키리코는 `2×2 + 0 + 0 + 3 + 0 = 7`이다. 각각 0.4의 5점에서 의도대로 변경된다.
- 같은 조합에서 탱커 라마트라 `0.5×3 + 0 + 2 - 2 + 2 = 3.5`, 로드호그 `2×3 + 0 + 3 - 3 + 2 = 8`은 그대로다. 딜러·힐러에 소수 항을 쓴 별도 고정 데이터도 검증한다.
- 등급은 세 역할의 모든 경계에서 바로 아래·정확히 경계·바로 위를 검사한다. 힐러 0.5점은 중립, 0.5001점은 약간 유리다. 후보가 하나가 되어 1위가 되어도 등급은 바뀌지 않는다.
- 필터는 0명/전체 선택의 동등성, 1~2명 후보, 원래 상위 3명 밖의 후보 포함, 동점 고정 순서와 순위, 점수 불변을 검증한다.
- `npm run build`: TypeScript 검사와 Vite 빌드 통과.
- `npm run portable`: 오프라인 단일 HTML 생성 완료. 파일 주소에서 별도 DOM 실행으로 53개 초상화 내장, 저장 차단 안내, 선호 필터와 등급 표시를 확인했다. 등급 글자(25px)가 점수(14px)보다 크게 적용된다.
- 프로덕션 빌드를 별도 임시 JSDOM 환경에서 실행했다. 세 단계 화면, 순위 선택·이동·전체 선택·해제, 건너뛰기, 1~2명만 완료, 필터·등급 표시, 역할별 재방문 복원, 상대 초기화 후 선호 유지, 일부/전체 상대 유지, 저장 차단 시 동작을 확인했다. DOM 실행 오류 0개. 프로젝트에 JSDOM 의존성은 추가하지 않았다.
- 위 조합의 실제 화면 검증 결과: 탱커 `D.Va 9·로드호그 8·레킹볼 6`, 딜러 `정크랫 8·리퍼 8·메이 7`, 힐러 `키리코 7·바티스트 5·젠야타 5`. 정크랫과 리퍼는 공동 1위, 바티스트와 젠야타는 공동 2위다. 고정 경계에 따라 이 조합의 딜러 상위 3명은 모두 “유리”다.
- 로컬 개발 주소는 실제 브라우저에서 `ERR_BLOCKED_BY_CLIENT`로 차단된다. **0.5의 PC·모바일 시각 검증은 아직 수행하지 못했다.** DOM 검증을 실제 렌더링 검증으로 간주하지 않는다.
- `git diff --check` 통과. 상성 데이터·영웅 목록·기존 회귀 자료는 무변경이고 등급 JSON은 첨부와 바이트 단위로 같다.

## 배포 상태와 다음 확인

- 저장소: `github.com/godmooner/ow-coach`. 공개 주소: `https://godmooner.github.io/ow-coach/`.
- 0.4는 사용자가 업로드한 PR #1이 병합되어 `main`의 `dfd2171`이 되었다. Actions 실행 `35427749481`의 검사·빌드·Pages 배포 성공을 확인했다. 이전 STATE의 “0.4 미반영” 기록을 정정한다.
- 공개 0.4 사이트에서 역할 선택·세 역할 전환·상대 5명 유지·추천 결과를 실제 브라우저로 확인했다. 이 확인은 새 0.5 화면의 검증과 구분한다.
- **이번 0.5는 아직 원격 반영·배포하지 않았다.** 이전 GitHub 쓰기 요청이 `403: Resource not accessible by integration`으로 거부되어 업로드용 ZIP으로 변경 파일을 전달한다.
- 변경 파일을 새 브랜치로 업로드해 PR을 만들고 `main`에 병합하면 기존 워크플로가 검사·빌드 후 배포한다.
- 반영 후 PC·모바일에서 모스트 화면, 이름이 긴 영웅과 전체 선택 요약, 등급·작은 점수의 배치를 확인한다. 선호 0명·1명·여러 명, 초기화, 새로고침 후 복원도 직접 확인한다.

## 다음 버전에 대한 의견

- 가중치와 고정 경계선은 실제 사용 피드백으로 함께 조정하는 편이 좋다.
- 선택 순서 배열과 명시적인 모스트 순위를 둘 다 보존했으므로 다음 순위 활용 기능은 의미를 먼저 확정해야 한다.
- 현재 규모에서는 역할별 React 상태와 localStorage로 충분하며 추가 상태 관리 라이브러리는 필요하지 않다.
