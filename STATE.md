# 현재 상태 — v1.5.1

이번 요청은 전장 우선 선택, 선택 완료 시 목록 접기, 상대·아군 공용 초상화 선택창이다. 아군 궁합 데이터 생성·점수 반영은 보류한다.
2026-09-21 읽기 전용으로 확인한 GitHub main은 `e8d52cfcf0f652a32b5edab2b34e0a4b33e7131d`이며, PR #5로 v1.5 데이터·선택형 선호 영웅 업데이트가 병합돼 있다. 이번 파일은 그 위에 적용하는 v1.5.1 화면 패치다. GitHub에 직접 쓰거나 배포하지 않았다.

## 이번 반영 내용

- 역할 선택 후 **전장 → 상대 조합 → 아군 조합** 순서로 표시한다.
- 전장은 상대 선택 전에 고를 수 있다. 모드·썸네일을 선택하면 이름·작은 사진만 남기고 목록을 접는다. 상대가 미완성일 때는 상대 선택창을 연다. 전장 없이 진행할 수도 있다.
- 상대·아군은 하나의 공용 초상화 선택 컴포넌트를 사용한다. 동시에 펼쳐진 입력창은 하나뿐이다.
- 상대 5명·아군 4명을 채우면 자동으로 접는다. 일부만 선택해도 완료·접기로 닫을 수 있다. 변경 버튼·빈 슬롯으로 열고, 채워진 슬롯으로 해당 영웅을 해제한다.
- 아군은 항상 5칸을 보여주되 본인 역할의 첫 칸을 `내 자리`로 예약한다. 탱커는 딜2·힐2, 딜러는 탱1·딜1·힐2, 힐러는 탱1·딜2·힐1을 선택한다.
- 역할을 바꿀 때 기존 아군을 새 정원에 맞춰 정리한다. 먼저 고른 영웅부터 유지하고 초과 인원만 해제한다. 중복·잘못된 ID는 제외한다.
- 모스트 화면 왕복 시 상대·아군·전장을 유지한다. 각 팀 초기화는 해당 팀만 비운다. 아군·상대·전장은 현재 페이지에서만 유지한다.
- 내장 한글 글꼴에 빠져 있던 글자를 보충했다. 외부 한글 글꼴에 의존하지 않고 현재 화면 문구·영웅·전장·등급을 표시한다.
- 표시 버전·패키지 버전은 v1.5.1 / 1.5.1이다. 새 런타임 의존성은 없다.

## 유지한 계산과 데이터

`src/recommend.ts`, `src/recommendationView.ts`, `src/weights.ts`와 모든 `data/` 파일은 이번에 변경하지 않았다. 기존 데이터 보정을 추가하거나 되돌리는 작업은 포함하지 않는다.
아군 ID는 화면 상태에만 있으며 추천 함수에 전달하지 않는다. 아군 점수·가중치·등급 재산출·아군 선택에 따른 후보 제외도 적용하지 않는다.

v1.5의 선호 미설정 시 전체 역할 추천, 모스트+가능 중 최대 3명, 후보 1~2명이면 그 인원만 추천, 전부 약간 불리 이하면 전체 최고 영웅 추가 제안 규칙을 유지한다.
기존 기본 가중치는 상성 1·궁합 0·맵 0.6이다. 상대 탱커 항은 내가 탱커일 때 2배, 딜러/힐러일 때 1.5배다. 고정 등급 경계와 동점 처리도 유지한다.
데이터 배경과 기존 v1.5 연구 패치는 `docs/v1.5.md`를 참고한다. 아군 궁합 Excel은 이번 패치의 입력 자료가 아니다.

## 구조와 다음 작업 위치

- `App.tsx`: `activeInput`으로 전장/상대/아군/접힘 상태를 관리한다. 기존 상대 ID 배열과 별도로 `allyIds`를 유지한다.
- `MapPicker.tsx`: 전장 목록·접기·이름/작은 사진 요약.
- `TeamPicker.tsx`: 팀별 5칸 요약과 공용 영웅 선택창. 본인 자리는 잠긴 역할 아이콘이다.
- `allySelection.ts`: 전체 역할 정원에서 본인 1칸을 빼는 계산, 아군 추가/해제, 역할 변경 시 정리.
- `tests/allySelection.test.ts`: 역할 정원과 모든 역할 전환 회귀 검사.
- 자세한 UX 규칙·한계·후속 연결 지점: `docs/v1.5.1.md`.

## 검증

- `npm test`: 기존 64개 + 새 7개 = 71개 통과. 현재 main에 전달 대상 12개 파일만 덮어쓴 작업본에서도 71개 테스트와 빌드가 통과했다.
- `npm run build`, `npm run portable`, `npm run grades:check` 통과.
- 실제 HTML의 DOM 검사: 전장 우선, 자동/수동 접기, 하나의 선택창, 본인 자리, 역할 변경, 전장 건너뛰기/해제, 팀 초기화, 모스트 왕복 보존을 확인했다.
- 변경 전 v1.5의 세 역할 추천을 저장해 같은 상대·전장에서 대조했다. 아군 선택 전후에도 영웅·점수·등급·순위가 동일했다.
- 기존 선택형 모스트·역할별 저장·가능 포함·1~2명 추천·복원 흐름도 통과했다.
- Chromium의 1440·768·390·320px 화면 폭에서 가로 넘침·JavaScript 오류가 없었다. PC·모바일 스크린샷과 한글 글리프 포함 여부를 확인했다.
- 실행: `npm ci`, `npm run dev`. 배포 빌드: `npm run build`. 단일 HTML: 빌드 후 `npm run portable`.

## 사용자 업데이트

`OW_Coach_v1.5.1_Update.zip`의 `UPLOAD_FILES` 안에 있는 폴더와 파일을 저장소 루트에 업로드하고 새 브랜치/PR로 병합한다. 상성·맵·등급 데이터나 배포 workflow는 이 ZIP에 포함하지 않는다. 자세한 클릭 순서는 함께 전달하는 `GitHub_v1.5.1_Update_Guide.md`를 따른다.
현재 사이트에 적용되는 시점은 사용자가 GitHub에서 병합하고 기존 배포 작업이 완료된 이후다. 전달한 단일 HTML로 먼저 로컬에서 새 UX를 확인할 수 있다.
