# 현재 상태 — v1.0.1

기준: `main`의 `0869b1d` (v1.0 업로드). 이전 v1.0 전달본과 원격 main의 파일 트리가 동일함을 확인했다.
작업 브랜치: `codex/v1.0.1-most-priority`.
요청: 대화의 모스트 표시·추천 대상 변경 패치. 맵·가중치는 기존 `HANDOFF-v1.0(1).md` 구현을 유지한다.
시작 전에 STATE·추천·모스트 코드를 읽고 원격 main을 갱신했다. v0.5와 v1.0은 모두 원격 main에 반영되어 있다.

## 이번 버전의 범위

- 화면의 4순위 명칭을 모두 ‘가능’으로 바꿨다. 모스트 선택 카드 1~3순위에는 선택한 영웅의 초상화와 이름을, 가능에는 이름만 표시한다. 최대 1명·여러 명 선택 가능 부제목은 없앴다.
- 주 추천은 명시적인 모스트 1~3순위 안에서만 점수순으로 표시하고 이름 옆에 개인 모스트 순위를 붙인다.
- 주 추천이 한 명 이상이며 모두 ‘약간 유리’ 이하이면 가능 중 최고 점수 한 명을 별도로 표시한다. 한 명이라도 ‘유리’ 이상이면 숨긴다. 가능 후보가 없으면 표시하지 않는다. 추가 추천은 주 추천보다 좋은 점수일 필요는 없다.
- 주 추천 0명은 모스트 선택 안내, 1~2명은 그 인원만 표시한다. 전체 선택으로 가능만 채운 경우도 주 추천으로 확대하지 않는다.
- 원점수·계산·고정 등급·동점 순서·맵·가중치·저장 스키마는 그대로다. 이전에 저장한 4순위 영웅은 자동으로 ‘가능’으로 표시된다.
- 화면·패키지 버전은 v1.0.1 / 1.0.1이다.

## 유지한 v1.0 기능

- 기존 역할 → 모스트 → 상대 선택을 유지하고, 상대 5명 완성 후 모드 → 전장 선택을 추가했다.
- 상성·궁합·맵의 영역별 가중치를 합산에 실제 적용하고, 개발용 가중치 설정 패널을 추가했다.
- JSON 3개와 썸네일 30개를 첨부 내용 그대로 배치했다. 등급 파일은 기존 것을 교체했다.
- 아군 선택·궁합 데이터, 모스트 순위 가산점, 검색·단축키·URL 공유, 초상화 최적화는 추가하지 않았다. 기존 화면 톤을 유지한다.

## 맵 데이터와 선택 화면

- `data/build/maps.json`: 전장 30개. 쟁탈 7 / 호위 8 / 혼합 8 / 밀기 4 / 플래시포인트 3.
- `data/build/map_scores.json`: 영웅 53명 × 전장 30개, 총 1,590개 값. 모든 값은 유한한 −3~+3 숫자이며 소수를 허용한다. 기존 영웅 ID와 전장 ID를 그대로 사용한다.
- 점수의 배경: 첨부 문서는 맵별 픽률을 전체 픽률과 비교하고 영웅·전장 전체 효과를 제거한 값을 표준화했다고 설명한다. 앱에서 원시 통계를 다시 수집하거나 점수를 재산출하지 않았다.
- `public/maps/<id>.webp`: 원본 ZIP의 30개 파일을 그대로 추출했고 모두 640×360 WebP임을 확인했다.
- `src/MapPicker.tsx`: 상대 목록 아래에 위치한다. 상대 5명 완성 전에는 안내만, 완성 후에는 모드 버튼 5개를 표시한다. 첫 모드를 임의로 고르지 않으며 모드를 누르면 그 모드의 전장만 표시한다.
- 썸네일 영역은 16:9다. 이미지 로딩이 실패하면 단색 카드와 한글명으로 선택할 수 있다. 선택은 별도 상태라 이미지 오류와 무관하다.
- 맵은 선택 사항이다. 안 골라도 상대 5명이 완성되면 추천하며 맵 항은 0이다.
- 모드 변경은 표시 목록만 바꾼다. 현재 맵은 유지되고 선택한 전장의 이름을 계속 표시한다.
- 상대 초기화·역할 변경·모스트 수정 중에도 선택한 맵을 유지한다. “전장 해제”가 맵만 비운다. 상대와 맵은 현재 페이지에서만 유지하며 새로고침 시 새 게임으로 시작한다.

## 계산 구조와 등급

```text
총점 = W_matchup × Σ(상대 5명 상성 × ENEMY_WEIGHT[내 역할][상대 역할])
     + W_synergy × 아군 궁합 합계
     + W_map     × 해당 영웅의 전장 점수
```

- `src/recommend.ts`의 `BLOCK_WEIGHT`: `{matchup: 1, synergy: 0, map: 0.3}`.
- `ENEMY_WEIGHT` 기본값: 내 역할이 탱커일 때 상대 탱커 3배, 딜러·힐러일 때 상대 탱커 2배. 상대 딜러·힐러 항은 모두 1배다.
- `recommend(..., options?)`의 선택 인자로 `mapId`, `mapScores`, `weights`를 받는다. 기존 4개 인자로 호출하면 기본 가중치·맵 없음으로 동작한다.
- `ScoreBlocks`와 `combineScoreBlocks()`에 `synergy` 항이 포함되어 있다. 현재 궁합 합계는 0이다. 다음 버전에서 아군 데이터·합계 계산을 연결하면 같은 합성 경로로 가중치를 적용할 수 있다.
- 미러 0점, 누락 0점, 후보 역할 제한, 상대 1/2/2 검증, 소수 허용, 동점 같은 순위·영웅 목록 순서를 유지했다. 소수 슬라이더 곱셈의 미세한 오차는 총점 12자리에서 정리한다. 화면의 두 자리 표시값으로 정렬하거나 등급을 매기지 않는다.
- 전체 역할 후보의 점수 계산 후 `selectMostRecommendations()`에서 명시적 모스트 1~3순위와 가능 후보를 나눈다. 주 추천끼리 표시 순위를 다시 매기되 모스트 순위 가산점이나 동점 우선권은 없다. 가능 추가 추천 판단은 반올림 전 점수의 고정 등급(level ≤ 4)으로 한다.
- `grade_cuts.json`은 맵 항을 포함해 다시 산출한 첨부 파일로 교체했다. `block_weight`와 `enemy_weight`가 코드의 기본 상수와 일치한다.
- 등급은 표시 계층에서만 만든다. 고정 경계에 `score <= cut`을 적용하며 조합 내 순위·백분위로 만들지 않는다.
- 사용자가 가중치를 바꿔도 등급 경계는 고정된다. 패널에 조정 완료 후 경계선 재산출이 필요하다는 안내를 넣었다.
- **맵 미선택·기본 가중치에서는 v0.5와 점수·순서가 같지만, 등급 라벨은 새 경계선으로 바뀔 수 있다.**

## 개발용 설정

- `src/WeightSettings.tsx`: 처음에는 접힌 패널이며 톱니 모양과 “개발용 설정”으로 연다. 기본값과 다르면 접혀 있어도 “변경됨” 표시를 유지한다.
- 상성·맵 가중치: 0~2, 0.05 단위. 궁합은 값 0을 보여 주되 데이터 없음으로 비활성이다.
- 내 역할별 상대 탱커 가중치 3개: 0~5, 0.5 단위. 상대 딜러·힐러 칸은 변경하지 않는다.
- 슬라이더 변화는 즉시 추천에 적용된다. “기본값으로”는 전체 기본값을 복원하고 표시도 해제한다. 맵·상대·모스트 선택에는 영향을 주지 않는다.
- `src/weights.ts`: 기본값 복제, 변경 여부 판단, 저장·검증을 담당한다. 저장 키는 `ow-coach.weights.v1`, 내용은 `{block, enemy}`다.
- 숫자 범위·슬라이더 단위에 맞는 유한한 값만 복원한다. 이상한 JSON·알 수 없는 필드·비활성 궁합 값·조절 불가능한 상대 역할 칸은 허용된 기본값으로 처리한다. 유효한 다른 설정은 유지한다.
- 저장이 차단되어도 앱과 슬라이더는 동작하며 현재 페이지에서만 유지된다는 안내를 표시한다.

## 모스트 선택·저장 호환

- 1~3순위 각각 최대 한 명, 가능 여러 명. 빈 순위나 0~2명 선택도 완료할 수 있으나 주 추천에는 1~3순위 중 적어도 한 명이 필요하다.
- 전체 선택은 기존 상위 순위를 유지하고 나머지를 가능으로 넣는다. 전체 해제·선택 없이 건너뛰기는 현재 역할의 선호만 비운다.
- 주 추천 0명일 때 전체 후보로 확장하던 v0.5/v1.0 표시 규칙은 이번 요청으로 폐기했다. 점수와 등급은 선택 순위의 영향을 받지 않는다.
- `ow-coach.preferred-heroes.v1`에 역할별 클릭 순서 배열, `ow-coach.most-ranks.v1`에 역할별 명시적인 1~3순위를 별도로 저장한다. 새로 선택한 영웅은 배열 뒤에 추가하며, 순위 이동만으로 클릭 순서를 바꾸지 않는다.
- 잘못된 저장값·다른 역할·중복·알 수 없는 ID를 복원에서 제외하고, 브라우저 저장 차단 시에도 현재 페이지에서 동작한다. 상대 초기화는 모스트를 지우지 않는다.

## 0.4에서 확정한 데이터와 딜러 회귀 기준

- `data/build/matchups.json`을 첨부 파일과 바이트 단위로 동일하게 교체했다. `heroes.json`은 기준 커밋과 동일하다.
- 출처 영웅 53명, 점수 2,754개, 그중 소수 86개. 미러 외 누락은 도미나→캐서디 및 캐서디→도미나뿐이다.
- 기존 1,190개 관계는 모두 보존되며 값 불일치는 0건이다.
- **인수인계서의 회귀 전제를 정정했다.** 딜러에서 시작하는 관계 57개가 추가되었고, 그중 45개가 0이 아니다. 기존 결측 0점을 대체하므로 같은 계산 코드에서도 일부 딜러 결과는 바뀐다.
- 이 사실을 사용자에게 설명했고, 사용자가 **새 데이터 전부 반영**을 선택했다. 기존 딜러 관계만 적용하는 호환 처리는 넣지 않았다.
- 0.4 당시의 회귀 기준은 **같은 데이터를 넣으면 0.3 계산 코드와 딜러의 전체 점수·순위·동점 순서가 같다**이다.
- 수정 전 0.3 코드로 기존/신규 데이터 각각의 기준 결과를 저장했다. 16개 상대 조합이 영웅 53명을 모두 한 번 이상 포함하며, 각 조합의 딜러 24명 전체를 비교한다.
- 기준 자료: `tests/fixtures/matchups-v0.3.json`, `tests/fixtures/recommend-v0.3.json`. 실제 상성 데이터를 갱신할 때 신규 데이터 기준 결과도 검토해야 한다. 기존 데이터 기준 결과는 보존한다.

데이터 교체로 달라지는 예: 상대 `D.Mon·안란·시에라·미즈키·제트팩 캣`.
기존 데이터는 토르비욘 6점·리퍼 5점·바스티온 4점, 신규 데이터는 애쉬 10점·위도우메이커 7점·리퍼 5점이다. 가중치 변경으로 발생한 차이가 아니다.

**데이터 갱신 주의:** 0.4에서 첨부 JSON만 교체했고, 0.5와 v1.0에서는 상성 데이터를 변경하지 않았다. 기존 CSV·XLSX·원문/파서는 0.3 자료이므로 `npm run data:from-csv` 또는 기존 원문 재생성을 실행하면 신규 관계가 사라질 수 있다. 새 원천 자료와 동기화하기 전에는 이 JSON을 기준으로 사용한다.

## 검증 결과

- `npm test`: 55개 통과. 기존 48개 + 모스트 전용 추천·개인 순위·가능 추가 추천의 역할별 경계·동점·빈 선택·역할 제한 7개다. 새 경계선에 맞춰 기존 힐러 중립 경계 예시를 0.5에서 0.4로 갱신했다.
- v1.0.1 프로덕션 DOM 확인: 순위별 초상화, 가능 이름만 표시, 부제목 제거, 모스트 후보 제한·개인 순위, 가능 한 명 추가·숨김, 맵/가중치 변경 시 재판단, 빈 순위·가능만 전체 선택·부분 선택·저장 복원을 확인했다. 모스트가 비어 있으면 상대 선택 전부터 안내한다. 실행 오류 0개다.
- 최종 오프라인 HTML을 파일 주소로 실행해 내장 이미지, 개인 순위, 위도우메이커 5점에서 가능 추가·하바나 5.9점에서 숨김을 확인했다. 실행 오류 0개다.
- **계산 엔진 v0.5 회귀(화면 후보 제한은 이번 요청으로 변경):** 수정 전 main의 코드로 `tests/fixtures/recommend-v0.5.json`을 생성했다. 적 53명을 모두 포함하는 16개 조합에서 세 역할 후보 전체의 점수·순위·동점 순서를 비교한다. 맵 없음·기본 가중치에서 모두 일치한다. 기존 0.3·0.4 기준 자료도 보존했다.
- **실제 데이터 손계산:** 상대 `D.Mon·애쉬·겐지·아나·메르시`, 딜러 위도우메이커의 상성 합계는 `2×2 + 0 - 3 + 2 + 2 = 5`. 하바나(+3)는 `5 + 0.3×3 = 5.9`, 남극 반도(−3)는 `5 + 0.3×(-3) = 4.1`로 일치한다.
- 영역별·역할별 사용자 가중치, 모두 0인 경우, 누락 맵 항, 미러, 미완성 상대, 소수 계산과 동점 유지, 궁합 자리의 합성 경로를 검증했다.
- 데이터 검증은 정확히 53×30, 모든 ID 대응, 값 범위, 모드별 개수, WebP 파일 존재·형식을 확인한다. 고정 등급의 경계 바로 아래·일치·바로 위 테스트도 통과한다.
- `npm run build`: TypeScript 검사·Vite 빌드 통과. 런타임·개발 의존성 추가 없음.
- `npm run portable`: 단일 HTML 생성 통과. 파일 주소로 별도 DOM 실행해 초상화 53개·전장 썸네일 30개 내장, 16:9 스타일, 하바나 5.9점·고정 등급, 저장 차단 안내를 확인했다. DOM 실행 오류 0개다.
- 별도 임시 JSDOM에서 실제 프로덕션 번들을 실행했다. 적 5명 전후 모드 표시, 다섯 모드의 전장 개수, 맵 미선택 결과, 맵 반영, 모드 변경 시 유지, 이미지 오류 폴백, 슬라이더 실시간 계산, 접힌 상태 변경 표시, 기본값 복원, 재방문 설정 복원, 초기화·역할 변경·모스트 수정 시 맵 유지, 별도 맵 해제, 저장 차단 시 동작을 확인했다. DOM 실행 오류 0개다.
- 로컬 개발 주소는 실제 브라우저에서 차단되어 **v1.0의 실제 PC·모바일 시각 검증은 아직 수행하지 못했다.** DOM 검증을 실제 렌더링 검증으로 간주하지 않는다.
- 별도 코드 검토에서 모스트 미선택 안내를 상대 5명 완성 전에도 보여주도록 보완했다. 기존 `recommend.ts`·데이터·저장 처리·맵·가중치 파일과 회귀 기준 자료는 변경하지 않았다.

## 맵 데이터 갱신 방법

1. 점수만 바뀌면 `data/build/map_scores.json`을 새 파일로 교체한다. 영웅 ID·전장 ID와 `{영웅id: {전장id: 점수}}` 구조를 유지하면 앱 코드를 바꾸지 않아도 된다.
2. 새 분포로 등급 경계선을 산출한 경우 `data/build/grade_cuts.json`도 교체한다. 정식 기본 가중치를 바꾸면 코드의 `BLOCK_WEIGHT`·`ENEMY_WEIGHT`와 경계선 메타데이터도 함께 맞춘다.
3. `npm test`·`npm run build`를 실행한다. 실제 데이터 손계산 예제는 새 값으로 독립적으로 검산한다. 맵 미선택 v0.5 기준 자료는 그대로 보존한다.
4. 오프라인 HTML도 쓰면 `npm run portable`로 다시 만든다. 전장 목록·ID가 바뀔 때에만 `maps.json`과 대응 썸네일을 함께 갱신한다.
5. 브라우저에 저장한 사용자 가중치는 데이터 교체로 지워지지 않는다. 기본 결과 확인 시 개발용 설정의 “기본값으로”를 사용한다.

## 배포 상태와 다음 확인

- 저장소: `github.com/godmooner/ow-coach`. 공개 주소: `https://godmooner.github.io/ow-coach/`.
- 시작 시 원격 main `0869b1d`의 v1.0 반영과 이전 전달본의 동일성을 확인했다. v0.5는 PR #2(`378d0da`)로 반영된 상태다.
- **이번 v1.0.1은 아직 원격 반영·배포하지 않았다.** 기존 GitHub 연결 앱의 쓰기 권한 제한(403)이 있어 업로드용 ZIP으로 변경 파일을 전달한다. main에 병합하면 기존 Actions가 검사·빌드·Pages 배포를 수행한다.
- 단일 HTML 생성 도구에 전장 썸네일 내장을 추가했다. 썸네일 파일이 없을 때도 생성은 가능하며 앱의 이름 카드 폴백을 사용한다.
- 반영 후 PC·모바일에서 가로형 카드와 긴 전장명, 설정 패널의 슬라이더, 모스트·상대·전장 전체 흐름을 직접 확인한다.

## 다음 버전에 대한 의견

- 모드별 선택은 현재 요청대로 구현했다. 실사용에서 느리다면 최근 전장 바로가기를 검토할 수 있다.
- 맵 가중치와 등급 경계선은 실제 사용 기록을 보고 함께 조정하는 편이 좋다.
- 설정·맵 선택·계산을 분리했으므로 다음 아군 궁합 데이터는 합계 계산을 연결하는 방식으로 확장할 수 있다.
