# 현재 상태 — MVP 0.4

기준: `main`의 `235ffac55a90f65d3e362778beebe092e1f41725` 및 사용자가 제공한 `HANDOFF-v0.4.md`.
작업 브랜치: `codex/mvp-0.4-role-selection`. 확인 날짜: 2026-09-19.

## 완료

- 최초 화면에서 탱커·딜러·힐러를 선택하면 확인 버튼 없이 상대 선택 화면으로 이동한다.
- 현재 역할 표시와 역할 바꾸기를 추가했다. 역할을 바꿔도 일부 또는 전체 상대 선택이 유지된다.
- 추천 후보는 선택한 역할 안으로 제한한다: 탱커 15명, 딜러 24명, 힐러 14명 중 상위 3명.
- 상대 선택 규칙은 역할에 관계없이 탱커 1·딜러 2·힐러 2명이다. 선택·해제·초기화와 5명 완성 전 결과 숨김을 유지했다.
- `recommend(heroes, matchups, enemyIds, myRole)`에 역할 인자를 추가했다.
- `ENEMY_WEIGHT[내 역할][상대 역할]` 표에서 탱커→탱커만 3, 나머지 칸은 모두 1이다.
- 소수 점수, 미입력 0점, 미러 항 0점, 동점 같은 순위와 영웅 목록 순서를 유지했다.
- 기존 화면 톤·초상화·데이터 import 경로를 유지하고 역할 선택에 필요한 스타일과 문구만 추가했다. 버전은 0.4.0이다.
- 아군 궁합, 맵, 검색, 단축키, URL 공유, 추천 근거, 초상화 최적화는 변경하지 않았다.

## 데이터와 딜러 회귀 기준

- `data/build/matchups.json`을 첨부 파일과 바이트 단위로 동일하게 교체했다. `heroes.json`은 기준 커밋과 동일하다.
- 출처 영웅 53명, 점수 2,754개, 그중 소수 86개. 미러 외 누락은 도미나→캐서디 및 캐서디→도미나뿐이다.
- 기존 1,190개 관계는 모두 보존되며 값 불일치는 0건이다.
- **인수인계서의 회귀 전제를 정정했다.** 딜러에서 시작하는 관계 57개가 추가되었고, 그중 45개가 0이 아니다. 기존 결측 0점을 대체하므로 같은 계산 코드에서도 일부 딜러 결과는 바뀐다.
- 이 사실을 사용자에게 설명했고, 사용자가 **새 데이터 전부 반영**을 선택했다. 기존 딜러 관계만 적용하는 호환 처리는 넣지 않았다.
- 회귀 기준은 **같은 데이터를 넣으면 0.3 계산 코드와 딜러의 전체 점수·순위·동점 순서가 같다**이다.
- 수정 전 0.3 코드로 기존/신규 데이터 각각의 기준 결과를 저장했다. 16개 상대 조합이 영웅 53명을 모두 한 번 이상 포함하며, 각 조합의 딜러 24명 전체를 비교한다.
- 기준 자료: `tests/fixtures/matchups-v0.3.json`, `tests/fixtures/recommend-v0.3.json`. 실제 상성 데이터를 갱신할 때 신규 데이터 기준 결과도 검토해야 한다. 기존 데이터 기준 결과는 보존한다.

데이터 교체로 달라지는 예: 상대 `D.Mon·안란·시에라·미즈키·제트팩 캣`.
기존 데이터는 토르비욘 6점·리퍼 5점·바스티온 4점, 신규 데이터는 애쉬 10점·위도우메이커 7점·리퍼 5점이다. 가중치 변경으로 발생한 차이가 아니다.

**데이터 갱신 주의:** 이번 작업은 첨부 JSON만 교체했다. 기존 CSV·XLSX·원문/파서는 0.3 자료이므로 `npm run data:from-csv` 또는 기존 원문 재생성을 실행하면 신규 관계가 사라질 수 있다. 새 원천 자료와 동기화하기 전에는 이 JSON을 기준으로 사용한다.

## 확인 결과

- `npm test`: 13개 통과. 기존 4개 테스트, 기존 1,190개 값 보존, 동일 데이터 딜러 회귀, 세 역할 후보 제한, 탱커 가중치, 딜러·힐러 무가중치, 모든 역할의 미러·결측·동점·상대 구성 검증을 포함한다.
- 손계산: 라마트라 대 `D.Mon·애쉬·겐지·아나·메르시`는 `0.5×3 + 0 + 2 − 2 + 2 = 3.5점`. 같은 조합에서 로드호그는 `2×3 + 0 + 3 − 3 + 2 = 8점`이다. 실제 데이터와 결과가 일치한다. 상대 입력 순서를 뒤집어도 같다.
- 딜러·힐러에는 동일한 고정 항 `0.5 − 1 + 2 − 2 + 3 = 2.5점`을 각각 적용하는 테스트가 통과했다. 딜러·힐러 항끼리 상쇄되지 않아 다른 역할에 가중치가 잘못 붙는 경우도 검출한다.
- `npm run build`: TypeScript 검사와 Vite 빌드 통과. 런타임 및 프로젝트 개발 의존성 추가 없음.
- 프로덕션 빌드를 별도 JSDOM 환경에서 실행해 최초 역할 선택, 자동 전환, 일부/전체 상대 선택 보존, 역할별 상위 3명과 점수·문구, 상대 역할 제한, 해제·초기화, 초상화 오류 폴백을 확인했다. DOM 실행 오류 0개. 이 검증은 `npm test` 13개와 별도로 수행했다.
- 실제 브라우저는 로컬 개발 주소를 `ERR_BLOCKED_BY_CLIENT`로 차단했다. 이번 변경의 PC·모바일 시각 검증은 수행하지 못했으며, 기존 0.3의 브라우저 검증을 0.4 검증으로 간주하지 않는다.
- `git diff --check` 통과. 첨부 JSON 동일성, 영웅 목록 무변경, 0.3 데이터 기준 자료의 원본 동일성을 확인했다.

## 배포와 다음 확인

- 저장소는 `github.com/godmooner/ow-coach`이며, 인수인계서에 따르면 기존 0.3은 `https://godmooner.github.io/ow-coach/`에 이미 배포되어 있다. 이전 문서의 “저장소 미연결·미배포” 기록을 정정했다.
- 이번 0.4는 로컬 작업 브랜치의 검토 대상이다. GitHub 연결 앱의 쓰기 요청이 `403: Resource not accessible by integration`으로 거부되어 원격 브랜치 반영과 PR 생성은 완료하지 못했다. 별도 패치 `ow-coach-v0.4.patch`로 변경을 전달한다.
- `main` 병합 및 실제 Pages 배포는 별도다. 기존 워크플로는 `main` 푸시 시 검사·빌드 후 배포한다.
- 남은 확인: 실제 PC·모바일 브라우저에서 역할 선택 화면과 역할 변경 동작을 시각적으로 확인한다.
