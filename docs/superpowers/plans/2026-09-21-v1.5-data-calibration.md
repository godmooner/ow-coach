# v1.5 Data Calibration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver the reviewed 53×53 matchup and 53×30 map data as Excel, and a working v1.5 recommendation patch with calibrated fixed grades.

**Architecture:** Preserve the existing additive recommendation engine, role/most selection and stable ties. Apply the 18 explicit teamfight corrections from the completed validation, reduce enemy-tank emphasis, raise map emphasis, and generate fixed role-specific grade thresholds using the same production scoring function.

**Tech Stack:** Existing React / TypeScript / Node 24 app; Python standard-library CSV conversion; Artifact Tool for Excel export.

**Spec:** Latest user request in this conversation: apply the completed research, provide matchup/map Excel, finish a usable patch, call it v1.5. The correction ledger is `data/revisions/v1.5-matchup-corrections.json` (copied from the completed research).

## Global Constraints

- Version 1.5.0; baseline origin/main 13b1b50, same tree as local v1.0.1.
- Matchup/map block weights 1 / 0.6; synergy remains 0.
- Enemy tank weights: tank 2; damage/support 1.5. Other enemy terms 1.
- Preserve missing versus neutral in Excel; mirrors 0; two missing directed relations remain absent in JSON and contribute 0 at runtime.
- Retain all 1,590 map values. Map data is pick-rate-derived preference, not win probability.
- No ally-selection or composition-classifier feature; do not change most selection, tie rules or UI tone.
- Fixed grade thresholds from 30,000 seeded valid enemy teams, balanced across 30 maps. Percentiles 5/20/40/60/80/95 over all candidates per role, independent of the current recommendation ranking.
- Existing exact legacy-default settings migrate automatically; genuine custom settings and all most selections persist.
- No deployment or merge as part of this task.

## Review Focus

1. Existing browsers must not silently retain obsolete defaults; preserve deliberate custom settings.
2. Grades must match data and weights and remain fixed when most preferences change.
3. CSV conversion must not drop tank/support rows or silently replace the full matrix with old damage-only data.
4. Workbook blank cells must not be mislabeled as evidence of neutrality; all exported numeric data must match the app.
5. Portable HTML must contain the new engine/data/images and use the same recommendation behavior as the built app.

### Task 1: Data, scoring and calibration

**Files:** `data/build/matchups.json`, `matchups.csv`, `grade_cuts.json`; `data/revisions/v1.5-*`; `src/recommend.ts`, `src/weights.ts`; `scripts/calibrate_grades.ts`, `scripts/csv_to_json.py`; affected tests and historical fixtures.

**Interfaces:** Existing `recommend(heroes, matchups, enemies, role, options)` stays unchanged. New `calibrateGrades(...)` produces the JSON consumed by `gradeForScore`. `npm run grades:calibrate` regenerates and `npm run grades:check` checks the shipped artifact.

- [ ] Write `tests/v1.5.test.ts` with literal hand-checked expectations: Circuit Royal versus Sigma/Widowmaker/Ashe/Zenyatta/Illari gives Sigma 9.8, Ball 9.46, Winston 8.76; Gibraltar versus Zarya/Reaper/Bastion/Ana/Zenyatta gives Sigma 9.26, Winston 1.8. Test old settings migration and preservation of custom settings.
- [ ] Run `node --experimental-strip-types --test tests/v1.5.test.ts`; expect assertion failures under v1.0.1, rather than import errors.
- [ ] Preserve historical data/weights as explicit fixtures, then apply 18 corrections and the new defaults. Version the weights storage key and normalize legacy/default/custom payloads.
- [ ] Implement seeded calibration and a safe all-role CSV round trip. Record source hashes, scoring weights, sampling procedure and observed grade frequencies in the calibration JSON.
- [ ] Run `npm test` and `npm run grades:check`; expect all tests and deterministic recalibration to pass. Historical tests run against explicit historical parameters, not changed defaults.
- [ ] Commit Task 1, recording red/green evidence.

### Task 2: Release and Excel

**Files:** `package.json`, lockfile, `src/App.tsx`, `STATE.md`, `README.md`, `docs/v1.5.md`; workbook and release artifacts outside tracked source.

**Interfaces:** Consume the exact final build JSON, correction ledger and grade cuts from Task 1. Workbook worksheets expose numeric 53×53 and 53×30 tables, corrections and settings/grades with labels.

- [ ] Set 1.5.0/version display; explain the 18 corrections, unchanged map values, weights, fixed percentile grades and migration in docs. Document safe CSV editing and grade regeneration.
- [ ] Create one Excel workbook using Artifact Tool; freeze names/headers, distinguish two missing cells, include IDs and corrections. Render each worksheet; compare all values against JSON and check formulas.
- [ ] Run `npm test`, `npm run build`, `npm run portable`, `npm run grades:check`. Check the portable app's role → most → enemies/map → recommendation flow.
- [ ] Commit source; obtain one independent whole-branch review and resolve substantive findings. Deliver the workbook, portable HTML and source patch/archive; publish a feature branch/PR if GitHub write access permits. Do not merge or deploy.
